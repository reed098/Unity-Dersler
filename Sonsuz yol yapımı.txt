//Plane oluşturuyoruz. Plane nin içine box collider atıyoruz.
//box collideri ikinci plane nesnesinin ortasına gelecek şekilde ayarlıyoruz.
//Plane i duplicate ediyoruz. İçlerine aşağıdaki script nesnesini atıyoruz.

    private void OnTriggerEnter(Collider other)
    {
        if(other.gameObject.tag == "oyuncu")
        {
            print("çarptı");
            transform.position += new Vector3(0, 0, GetComponent<Renderer>().bounds.size.z * 2); 
	    //kaç adet yol nesnemiz varsa ona göre çarpacağız.
        }
    }